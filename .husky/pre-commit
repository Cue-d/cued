#!/bin/sh

# Frontend: lint-staged + typecheck
cd frontend
pnpm exec lint-staged
pnpm run typecheck

# Backend: ruff (auto-format and lint with fixes)
cd ../backend
uv run ruff format .
uv run ruff check --fix .

# Core: cargo fmt + clippy
cd ../core
cargo fmt
cargo clippy -- -D warnings