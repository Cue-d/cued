interface:
  display_name: "Cued"
  short_description: "Inspect Cued's local SQLite store and manage contact memories."
  default_prompt: "Use $cued to inspect ~/.cued/local.db with sqlite3 -json for reads. Use cued contacts memory add/list/stale for successful useful contact memories."

policy:
  allow_implicit_invocation: true
